# Review: Add ConditionalAuthorization feature gate (db947349618)

- **SHA:** `db9473496188f606326eb2a49d090038632847a2`
- **Author date:** 2026-07-19
- **Subject:** Add ConditionalAuthorization feature gate
- **Reachable on:** `impl-conditional-authz-3` (current branch).
- **Size:** 4 files, +25 / -0

## What this commit does

Adds the `ConditionalAuthorization` feature gate:

- Const declaration in `staging/src/k8s.io/apiserver/pkg/features/kube_features.go:84` with the standard `// owner: @luxas` / `// kep: https://kep.k8s.io/5681` comment.
- Alpha, default off, at v1.37.
- Registered in `defaultVersionedKubernetesFeatureGates` and (empty) `defaultKubernetesFeatureGateDependencies` in `pkg/features/kube_features.go`.
- Corresponding entry in `test/compatibility_lifecycle/reference/{feature_list.md,versioned_feature_list.yaml}`.

## Files touched

- Production
  - `pkg/features/kube_features.go` (+6)
  - `staging/src/k8s.io/apiserver/pkg/features/kube_features.go` (+12)
- Documentation / codegen references
  - `test/compatibility_lifecycle/reference/feature_list.md` (+1)
  - `test/compatibility_lifecycle/reference/versioned_feature_list.yaml` (+6)

## Findings

### Critical (must fix before merge)

None.

### Important (should fix)

1. **The feature gate declaration lives in `staging/src/k8s.io/apiserver/pkg/features/kube_features.go` and is re-referenced from `pkg/features/kube_features.go` via the same name.** `staging/src/…/features/kube_features.go:84–90`. That's the correct pattern — the gate is defined in the shared `apiserver` package and lifted into the aggregator in `pkg/features`. Verify by grepping `ConditionalAuthorization` and confirming there's a single source of truth.
2. **Feature-gate description mentions "conditions are enforced during the validating admission phase" but doesn't reference the admission plugin name.** `staging/src/…/features/kube_features.go:87–90`. Consider naming `AuthorizationConditionsEnforcer` explicitly so operators reading `kubectl get --raw /metrics | grep feature` land on searchable terms.
3. **No lifecycle exit criteria noted.** For alpha → beta, the KEP defines graduation criteria. That's not a comment on the gate — it's just observational.

### Nits

- `test/compatibility_lifecycle/reference/feature_list.md` update — a table entry that will auto-render. Verified no manual formatting issue.
- The 6-line YAML addition in `versioned_feature_list.yaml` follows the exact schema of adjacent entries. Good.

### Questions

- Is there a plan to add the gate to `defaultKubernetesFeatureGateDependencies` in the future (e.g. depending on `AuthorizeWithSelectors`)? Currently empty — fine for alpha.

## What's well done

- Alpha default-off at introduction is the right conservative choice.
- The compatibility-lifecycle reference files are kept in sync in the same commit, avoiding a "gate exists but not documented" state.
- KEP link in the const godoc is exactly the format used elsewhere in the file.

## Tests

No hand-written tests touched. The commit adds a feature gate declaration in `staging/src/k8s.io/apiserver/pkg/features/kube_features.go:84–90` and mirrors it in `pkg/features/kube_features.go`. The gate is validated indirectly by the standard `feature-gate integrity` test harness (in `test/e2e/framework/*.go` and the compatibility lifecycle validator in `cmd/compat-lifecycle`), which verifies the gate is registered exactly once and that the reference files match the code. Any regression here would surface in CI's "codegen or reference-file drift" check. No commit-specific test to review.

## Verdict

**LGTM.** Small, atomic, follows conventions.
