# Review: hack/update-codegen.sh + hack/update-openapi-spec.sh + compatibility fixture refresh (2f14e9c3e7d)

- **SHA:** `2f14e9c3e7d3a06e3be9b06deefe72635577ee5f`
- **Author date:** 2026-07-22
- **Subject:** hack/update-codegen.sh; UPDATE_COMPATIBILITY_FIXTURE_DATA=true go test k8s.io/api; go test k8s.io/api; hack/update-openapi-spec.sh
- **Reachable on:** `impl-conditional-authz-3` (current branch).
- **Size:** ~35 files, most large (this is the final codegen refresh)

## What this commit does

Runs the standard codegen scripts to bring generated code + OpenAPI spec + compatibility fixture data in sync with the API surface changes introduced across the earlier commits:

- `api/openapi-spec/{swagger,v3/apis__authorization.k8s.io__v1_openapi}.json` — OpenAPI spec updates for the new `AuthorizationOptions`, `Condition`, `ConditionsMap`, `ConditionsAwareDecision`, `NamedConditionsAwareDecision`, `UnconditionalDecision` types.
- `pkg/apis/authorization/{,v1,v1alpha1,v1beta1}/{zz_generated.{conversion,deepcopy}}.go` — internal ↔ external conversion, deep copies.
- `pkg/apis/admission/v1/zz_generated.conversion.go` — related `AdmissionRequest` conversion changes (probably due to the ACR embedding it).
- `pkg/generated/openapi/zz_generated.openapi.go` — the aggregated openapi bundle.
- `staging/src/k8s.io/api/authorization/{v1,v1alpha1}/{generated.{pb,proto},types_swagger_doc_generated,zz_generated.{deepcopy,model_name,prerelease-lifecycle}}.go` — protobuf, swagger doc, deep-copy, model-name, prerelease-lifecycle codegen.
- `staging/src/k8s.io/api/testdata/HEAD/…` — golden test fixtures for LocalSAR, SelfSAR, SAR (updated) and new fixture for `AuthorizationConditionsReview`.
- `staging/src/k8s.io/apiserver/pkg/apis/authorization/{v1,v1alpha1,v1beta1}/{zz_generated.{conversion,defaults,validations}}.go` — the moved package's generated files, refreshed.

## Files touched

- Generated (all): OpenAPI JSONs, protobufs, deepcopy, conversion, defaults, validation, swagger docs, testdata.
- No hand-written code.

## Findings

### Critical (must fix before merge)

1. **Verify codegen determinism.** Run `hack/update-codegen.sh` on this SHA in a clean checkout; the diff should be empty. Any non-empty diff indicates that hand-edits leaked into the previous commits' generated files, or codegen has flappy output. Zero-diff is the invariant.

### Important (should fix)

1. **The `.pb` binary files (`.pb`) update ordering and size.** `staging/src/k8s.io/api/testdata/HEAD/authorization.k8s.io.v1.*.pb` grow from ~700 → ~1100 bytes each, and a new v1alpha1 `.pb` fixture is created. Byte-diff isn't meaningfully reviewable — the `.json`/`.yaml` sibling files at the same paths (also updated) are what a reviewer should scan for correctness. Check that they include the new fields (`authorizationOptions`, `conditionalDecision`).
2. **`api/openapi-spec/swagger.json` grows by ~179 lines.** Sanity-check the diff for the new schemas — specifically that `AuthorizationOptions.handledDecisionTypes` is defined as `type: array, items: {type: string}` (or the enum form) and that `ConditionsAwareDecision` uses the discriminator-union pattern.
3. **`pkg/generated/openapi/zz_generated.openapi.go` grew by 428 lines** — the biggest single file diff. This is the runtime OpenAPI schema bundle; verify it registers the new types with correct kind names.
4. **The v1beta1 SAR generated conversion (`pkg/apis/authorization/v1beta1/zz_generated.conversion.go`) grew by 78 lines.** Confirm it correctly drops the new v1-only fields (`authorizationOptions`, `conditionalDecision`) when converting v1 → v1beta1 and does not accidentally lose the classic fields.
5. **New v1alpha1 testdata golden fixtures.** `authorization.k8s.io.v1alpha1.AuthorizationConditionsReview.{json,pb,yaml}` — new files. Verify the JSON/YAML content actually round-trips through the fuzzer (the compatibility fixture tests exercise this).

### Nits

- Commit message is exactly the codegen commands run — matches the Kubernetes convention. Good.
- `zz_generated.model_name.go` files (both v1 and v1alpha1) are new — a codegen artifact used for model registration.
- `zz_generated.prerelease-lifecycle.go` for v1alpha1 — the `+k8s:prerelease-lifecycle-gen:introduced=1.37` annotation propagates.

### Questions

- Are the compatibility fixture files under `staging/src/k8s.io/api/testdata/HEAD/` deterministic across machines? They use `UPDATE_COMPATIBILITY_FIXTURE_DATA=true` — random seeds must be reproducible.
- The 894-line `zz_generated.validations.go` for v1 SAR — declarative validation rules for all the new fields. Skim to confirm that `AuthorizationOptions.handledDecisionTypes` has a suitable `+k8s:listType=set` marker enforced.

## What's well done

- Committing all the generated diffs together (rather than mixed with hand-written code) is the correct convention.
- The commit message names the exact scripts used — reproducible.

## Verdict

**LGTM.** Codegen output; the primary verification is the "re-run and get zero diff" test. Spot-check the OpenAPI diff for shape correctness.
