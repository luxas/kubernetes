# Review: Address liggitt's comments (27e0fa95ff9)

- **SHA:** `27e0fa95ff973b056e7eaf01fff18f95c918f74e`
- **Author date:** 2026-05-26
- **Subject:** Address liggitt's comments
- **Reachable on:** dangling; reachable by SHA only.
- **Size:** 4 files, +51 / -99

## What this commit does

Two review-driven simplifications:

1. Renames the predicate methods on `ConditionsAwareDecision`: `IsAllowed → IsAllow`, `IsDenied → IsDeny`. (`IsNoOpinion` is already Go-idiomatic and unchanged.) This aligns the method names with the `Allow`/`Deny`/`NoOpinion` variant nouns rather than the past-tense `Allowed`/`Denied` of the classic `Decision` enum.
2. Removes two APIs previously exported on `ConditionsAwareDecision`: `UnconditionalParts()` and `FailClosedDecision()`. Callers that need to bridge back to the classic `(Decision, string, error)` triple now do it inline (see the sample in `metrics_test.go:166–175`).

Also removes the invariant comment `INVARIANT: Exactly one of Is* must return true at all times.` that was incorrect (see review 01 finding).

## Files touched

- Production: 2 files.
  - `staging/src/k8s.io/apiserver/pkg/authorization/authorizer/conditions.go` (renames + method removals).
  - `staging/src/k8s.io/apiserver/pkg/authorization/metrics/metrics.go` (usage sites updated).
- Tests: 2 files.
  - `conditions_test.go` (test-name-only churn; `wantFailClosedIsDeny` field removed).
  - `metrics_test.go` (dummy authorizer now inlines the fold).

## Findings

### Critical (must fix before merge)

None.

### Important (should fix)

1. **`UnconditionalParts` and `FailClosedDecision` come back later.** Later commits — notably `3033213ea26` (Pre-factor for conditional authz) — reintroduce `UnconditionalParts` (with an `expectConditional bool` parameter) and `FailureDecision`. Removing them here only to reinstate them a few commits later suggests the review discussion concluded "not yet, but do it right when you add it back." The commit body doesn't record that outcome. Consider a one-line note in the commit body: `Removed; will return with a richer signature in the ConditionsMap/Union commits.`
2. **The inline fold in `metrics_test.go:166–175` is now duplicated everywhere legacy `Authorize` needs to bridge from `ConditionsAwareDecision`.** The 10-line switch is not something every test/impl should repeat. If `UnconditionalParts` is reinstated later, sweep any hand-inlined copies at that point.
3. **Behaviour change: `UnconditionalParts` used to fail closed via the internal `FailClosedDecision()` (which returned `Deny` for `IsDeny` else `NoOpinion`).** In the test's replacement (`metrics_test.go:170–175`), the default case now returns `authorizer.DecisionDeny` unconditionally with a hardcoded reason. That's fine for the test dummy, but confirm this fail-closed rule is preserved in every real call site once the API returns.

### Nits

- The test field name change `wantIsAllowed`/`wantIsDenied` is not updated to `wantIsAllow`/`wantIsDeny` — the field names still use the older suffix while the method names are shorter. Tiny inconsistency; either rename the fields for symmetry or leave as-is with a comment.
- Test assertion strings still say `IsAllowed()` / `IsDenied()` (e.g. `conditions_test.go:139,146`) even though the method is now `IsAllow`/`IsDeny`. If the assertion fires, the error message would refer to a method that doesn't exist. Update to match.
- `metrics_test.go:171` — reason string `"failed closed: wanted to return a conditional decision, but called on the conditions-unaware method"` is longer than the one previously used by `UnconditionalParts` — arguably clearer, but audit logs will diverge subtly between now and when the API returns.
- Commit message `Address liggitt's comments` is opaque without the review thread. Consider a follow-up rebase to fold this commit into `6d78dfd60cf` before the PR opens.

### Questions

- What was liggitt's exact objection to `UnconditionalParts`/`FailClosedDecision`? Understanding the rationale would help avoid re-introducing the same issue when the methods reappear in `3033213ea26`.
- Was the rename `IsAllowed→IsAllow` motivated by symmetry with the const names (`DecisionAllow`) or with the wire enum (`ConditionsAwareDecisionType.Allow`)? Either way, the resulting API is cleaner.

## What's well done

- The rename `IsAllowed→IsAllow`/`IsDenied→IsDeny` is a genuinely nicer read at call sites (e.g. `decision.IsAllow()` vs `decision.IsAllowed()`), and it heads off the future confusion once `ConditionsMap` and `Union` variants appear that don't have a natural past-tense.
- Removing the broken invariant comment ("Exactly one of Is* must return true at all times.") is the correct fix; it was actively misleading.
- The commit is small and easy to audit; the touched surface exactly matches what the review probably asked for.

## Tests

### Test files touched
- `conditions_test.go` — 79 lines updated / removed (`wantFailClosedIsDeny` column and its per-case assertion deleted; `IsAllowed()`/`IsDenied()` method references renamed to `IsAllow()`/`IsDeny()`).
- `metrics_test.go` — 11 lines updated (mirror rename in the `dummyConditionalAuthorizer.Authorize` implementation, from `unconditionalParts()` chained call to an explicit switch).

### Coverage
- **Well covered:** the rename pass keeps the table entries and the assertion logic; the existing test cases (zero-value, deny, allow, no-opinion, from-parts:unsupported-mode) still fire against the renamed methods.
- **Missing / thin:**
  - The removal of `FailClosedDecision()` means the previously-asserted `wantFailClosedIsDeny` column is silently dropped. Since the method was public and could have external callers, an in-tree test asserting `IsUnconditional()` behaviour would help catch any accidental reintroduction of the old naming. Not a coverage regression per se — the removed asserts were paired with the removed function.
  - The new `metrics_test.go` inlines a 10-line switch in `dummyConditionalAuthorizer.Authorize` (`metrics_test.go:166–176`) that reimplements what `UnconditionalParts` used to do. If `UnconditionalParts` returns in a later commit (spoiler: `3033213ea26` does re-export it), this inline copy could drift. Consider whether the test should re-use the reintroduced helper once available.
- **Feature-gate on/off:** N/A.

### Structure
- The `wantIsAllowed` / `wantIsDenied` field names were NOT renamed to `wantIsAllow` / `wantIsDeny` even though the methods were. Field names still use the past-tense suffix while the methods no longer do — small consistency wart. Not blocking.
- Test error messages like `t.Errorf("IsAllowed() = %v, want %v", isAllowed, tt.wantIsAllowed)` (`conditions_test.go:139`) still reference the old method names — those *would* be printed on failure, misleading a reader. See "Stale comments" below.

### Stale comments
- Test file: `conditions_test.go:139, 146` — assertion `t.Errorf` messages still say `IsAllowed()` / `IsDenied()` after the method rename. If a test fails, the error string references a method that doesn't exist. Fix.
- Production `conditions.go:170–197` — the `String()` method still contains a `Deny is written such that if none of the other modes apply, IsDenied() is true.` comment (added in commit `a`, not touched here). After the rename, this reads `IsDenied()` but the method is `IsDeny()`. Ripple stale comment.
- Test file: `metrics_test.go:171` — reason string `"failed closed: wanted to return a conditional decision, but called on the conditions-unaware method"` is longer than the one used by `UnconditionalParts` — divergence noted; audit logs may render slightly differently between now and reintroduction.

## Verdict

**LGTM.** Simple, review-driven cleanup. The reinstatement in later commits is a slight process concern (commit history churn), but the end state is cleaner than before.
